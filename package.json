{
  "name": "range-log",
  "version": "1.0.0",
  "description": "A range log for muzzle loaders",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/server.js",
    "develop": "export NODE_ENV=development && concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "prodtest": "export NODE_ENV=production && cd client && npm run build && cd ../server && npm run watch",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/melliott7264/range-log.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/melliott7264/range-log/issues"
  },
  "homepage": "https://github.com/melliott7264/range-log#readme",
  "dependencies": {
    "concurrently": "^7.3.0"
  }
}
