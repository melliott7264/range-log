import React, { useState, useEffect } from 'react';
import { useQuery } from '@apollo/client';
import { GET_FIREARM } from '../utils/queries';

const SingleFirearm = (_id) => {};

export default SingleFirearm;
